# CPU-only research web service, pinned to the verified Python 3.13 environment.
# PyTorch/MPS training and local model weights are not installed on Render.
numpy==2.2.3
# core/__init__.py imports the RCPD tree implementation at runtime.
scikit-learn==1.9.0
fastapi==0.141.1
uvicorn==0.52.4
SQLAlchemy==2.0.52
psycopg[binary]==3.3.5
psycopg-pool==3.3.1
httpx==0.28.1
pydantic==2.13.4
# Freeze the CPU service's transitive dependencies to the locally tested set.
# This keeps independent Render builds from silently changing the resolver result.
scipy==1.18.1
joblib==1.5.3
narwhals==2.25.0
threadpoolctl==3.6.0
starlette==1.6.0
typing-extensions==4.16.0
typing-inspection==0.4.2
annotated-doc==0.0.5
click==8.4.2
h11==0.16.0
anyio==4.14.1
certifi==2025.1.31
httpcore==1.0.9
idna==3.10
annotated-types==0.7.0
pydantic-core==2.46.4
psycopg-binary==3.3.5
# SQLAlchemy additionally requires greenlet on Render's Linux x86_64 host.
greenlet==3.2.4; platform_machine == "x86_64" or platform_machine == "amd64" or platform_machine == "AMD64" or platform_machine == "aarch64"
