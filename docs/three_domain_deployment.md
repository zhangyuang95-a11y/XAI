# Three-domain release v3.5.1

Release: `policylens-three-domain-20260920.v3.5.1`
Site: https://policylens-warehouse-study.onrender.com/
Runtime commit: `cb9c2cb9b86e862c262befcccf398ff2827cc4f5`
Runtime source SHA-256: `4ba03f0cc4957e28ae1990dd192c39499fad653cc9a97bcd84fe80600b1c9239`

v3.5 deployed successfully at 2026-09-20 15:08:02 +08:00 (Render `dep-danobpjm8hqs73c09tdg`). The compatible v3.5.1 patch is Live: Render `dep-danp6p2jnfac739ct9m0`, triggered at 16:03:48 +08:00 and live at 16:05:33 +08:00. The public manifest matches the tested commit and source hash. All three entrances and health return 200; persistent storage, real-provider QA and pilot readiness are true.

## Changes

- Warehouse replays the 121-frame tutorial generated by the unmodified `de16551` historical source (`build_development_tutorial`, seed 40786, initial AI battery 35). The source commit was recorded at 2026-09-02 12:54:11 +08:00; the exact historical deployment time is not verified. Its original demonstration scores remain in the asset but are hidden during playback. Task physics, historical controller/Actor and task scoring remain unchanged.
- Warehouse explanations identify the current A/B target and actual path distance. Conflict explanations name a possible human action conditionally; no unsubmitted action is treated as known. Claims of safety check all five human commands through the real motion rules. Actual collisions carry server-confirmed attempted positions for the approach-and-rebound animation.
- Pong explains the intended joint contacts and simultaneous catch points. A highest-point claim is scoped to that visible arrival and still-feasible earlier commitments. The controller, physics and scoring are unchanged; 12,060 rollout steps match the prior implementation exactly.
- Kitchen has five visible orders in each task, with different recipe counts and sequence. Tomato/pepper preparation takes 3 interactions, egg 4, meat 5. Scoring is raw: +100 per served dish, -1 per step, -3 per discarded single ingredient, -10 per discarded combined/finished dish. Raw ingredients expire after 120 turns; completing preparation starts a fresh 80-turn clock for egg/meat and 60-turn clock for tomato/pepper. Spoiled items remain until physical disposal.
- An AI holding a finished dish while the handoff is occupied commits to carrying that dish to the trash, even if the counter later clears. After delivery it prioritizes collecting the next ready finished dish. Trash penalties appear in red and food identities remain traceable.
- All demos use continuous Start/Pause/Skip controls. Gameplay still advances only on input. Only the newest answer is shown, with larger dark text; prior dialogue remains recorded for follow-ups and export. Only Group A during active Task 2 can ask or view answers.

## Validation evidence

Evidence directory: `../analysis/three_domain_revision_20260920_v35/` (private credentials and synthetic session recovery material are excluded from Git).

- Initial v3.5 regression: 650 tests and 156 subtests passed. Compatible-patch three-domain regression: 674 tests and 156 subtests passed; final history/format refinements were then verified with 524 affected-module tests and 152 subtests. Includes process restart/export, A/B permissions, idempotency, old-record isolation, real domain trajectories, collision rendering and frontend network races.
- Whole-repository expansion: 1,609 tests and 156 subtests passed, five legacy checks failed. An isolated `git archive ea617f` reproduced the same five failures: pre-existing 2,000-line/old-directory architecture thresholds, missing Web Crypto in two old Pong Node test harnesses, and a stale legacy Warehouse UI string expectation. The implicated files are unchanged by this patch. The Warehouse environment itself remains in the active physical path and has its separate trajectory checks. No claim of an all-repository green build is made.
- Warehouse: historical source replay equals the shipped asset; unchanged physical/controller behavior is checked separately. Goal labels, route distances and exhaustive conflict checks use the real state.
- Kitchen: 72 development plus 72 held-out simulated cooperative trajectories all completed five dishes, using 267–282 steps, without spoilage, burning or disposal. The frozen 360-step budget and deadlines are common to both groups. These are simulations, not human performance measurements.
- QA: 26 distinct bilingual questions evaluated in 54 real-provider requests, preserving initial verbose/duplicate answers and their corrected reruns. The final requested examples passed factual and concision review. These samples do not guarantee correctness for every possible question.
- Local browser checks: all three demos start continuously, pause/resume, and skip to Task 1 turn 0; Kitchen shows the five-dish menu, recipe label, directional arrows, preparation progress and trash. Pong demonstration finishes without per-step confirmation.

## Production acceptance and explanation correction

- Six synthetic A/B production flows completed: 18 tasks and six questionnaires. All three entrances, release fingerprint, persistent storage and real-provider QA readiness were checked. Production assets matched the tested files.
- All 2,678 recorded transitions across 19 runs matched the fixed AI, physical states, observations and scores. The separate disposal probe produced actual -3 and -10 penalties. One empty HTTP 502 during an action was recovered by retrying the same command identity; no duplicate action appeared in the exported trajectories.
- The initial 28 production requests all returned answers, but independent content review accepted only 23 as complete: three omitted requested causal context, one ambiguously named an ingredient slot, and one mislabeled -2 points as -2 completed orders. These original outputs remain preserved.
- v3.5.1 separates actual completed-order deltas from score deltas, supplies missing coordination/deadline facts, names the precise ingredient slot and removes duplicate waiting text. It preserves the requested simulation horizon. Two successive batches of the same 28 exact questions, states, decisions and dialogue/history passed local real-provider semantic review. After the history refinement, a later batch returned 27 correct answers and one unavailable response caused by an extra provider format field. That failed response remains saved; the final patch sends this error through the existing single repair attempt, still strictly validates the result, and passed a targeted repeat of the failed case. Separate English/Chinese historical-slot repeats also passed after retaining and correcting an initial binding failure. These are local provider checks, not new production requests; they do not guarantee availability or correctness for every free-form question.
- Replaying the 2,678 production transitions under the patch found identical decisions, physical states, observations and scores, apart from two permitted ingredient-slot event text corrections.
- An independent read-only check found all 2,767 retained v3.4/v3.3 baseline records unchanged, with none missing. This checks earlier synthetic acceptance records and one archive fixture, not the entire research database.

## Final patch acceptance

- All six old v3.5 instances, 18 runs, six questionnaires, frames, enrollment metadata and 28 saved answers remain identical in the filtered production export. Existing cookies recover the same runs. An active old Pong run continued from turn 10 to 11 exactly once under a repeated command, retaining its original release label and answers.
- Six new production HTTP questions passed semantic verification: Warehouse coordination/why, Kitchen precise tomato slot/recipe order, Kitchen two-turn score/order change and coordination/why, plus two historical-frame Pong advice/comparison questions. All six kept the current task state and turn unchanged. The Warehouse/Kitchen probe's 510 actual action transitions matched the engine; all 536 requests succeeded.
- An online browser recovery showed the old Pong Task 2 at turn 11, a single latest answer for historical turn 8, larger dark explanation text, and the concurrent ball field. Continuous playback/pause/skip behavior was verified in the local browser against the same deployed frontend assets; this is distinguished from the online HTTP flow checks.
- The deployment started a new Render process at 16:04:57 +08:00. The predeployment probe recovered the exact same run, revision, turn 3 and state hash after deployment. A separate manual Restart action did not trigger because the browser menu repeatedly invalidated its controls. It is not claimed as completed. Final post-deployment verification at 16:24:17 +08:00 used 12 successful read-only requests: six old instances, 18 runs, six questionnaires, 2,619 frames and 28 questions retained their baseline values; the same turn-3 probe recovered unchanged.

## Data and research status

v3.5.1 resumes v3.5 sessions with their original release metadata and preserves saved answers. The v3.5 Kitchen rules are incompatible with pre-v3.5 saved game states. Earlier releases remain archived; no old state is converted into the new game, and new/old releases must not be pooled in analysis. The unpublished v3.4.1 QA patch is included in v3.5. The original working checkout and existing research records are preserved.

A 50% Task 2 improvement remains a human pre-experiment target. No new human effect has been measured or guaranteed. Deployment, simulation and human-study results are reported separately.

## Evidence index

- `analysis/three_domain_revision_20260920_v35/production_patch_deployment_receipt.json`: actual Render deployment, public version and entrance checks.
- `production_patch_semantic_http_v3_5_1_review.md` and `production_pong_patch_qa_v351_receipt.json`: six new production questions.
- `production_final_post_deployment_persistence_v3_5_1_receipt.json`: final read-only preservation check; independent manual restart explicitly false.
- `v351_explanation_patch_report.md`: local real-provider batches, retained failures and targeted corrections.
- `legacy_five_failures_review.md`: isolated reproduction of the five existing whole-repository test failures.
- `production_legacy_record_preservation.md`: earlier archived baseline preservation.

Evidence files are stored outside the Git working tree. Private session/recovery material is excluded from the report and repository.
