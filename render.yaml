services:
  - type: web
    name: policylens-warehouse-study
    runtime: python
    plan: free
    region: singapore
    buildCommand: python -m pip install -r requirements-render.txt
    startCommand: python -m ui.development_preview_server --host 0.0.0.0 --port $PORT
    healthCheckPath: /
